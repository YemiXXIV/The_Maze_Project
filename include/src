#ifndef MAZE_H
#define MAZE_H

#define MAP_WIDTH 20
#define MAP_HEIGHT 20

typedef struct {
    int width;
    int height;
    char **data;
} t_map;

t_map *maze_init(void);
void maze_free(t_map *maze);

#endif  // MAZE_H
